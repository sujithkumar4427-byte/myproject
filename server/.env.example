# Server
PORT=5000
NODE_ENV=development

# MongoDB — replace with your own connection string
MONGODB_URI=mongodb://localhost:27017/aura-ecommerce

# JWT — generate a strong random secret before production
JWT_SECRET=your_jwt_secret_here
